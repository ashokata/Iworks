# API Configuration
# Important: Copy this file to .env.local and update with your actual API endpoint

# FieldSmartPro Backend API (after deploying infrastructure)
NEXT_PUBLIC_API_BASE_URL=https://your-api.execute-api.us-east-1.amazonaws.com/prod

# For local development with API running locally
# NEXT_PUBLIC_API_BASE_URL=http://localhost:3001

# Tenant ID (get this from your database after creating a tenant)
NEXT_PUBLIC_TENANT_ID=your-tenant-id

# AWS Bedrock (AI Chat) - Uses AWS SDK, no key needed if using IAM roles
# If running locally, configure AWS credentials via AWS CLI

# Mapbox Access Token (for maps)
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=your-mapbox-token-here

# Auth0 Configuration (optional - if using Auth0)
# NEXT_PUBLIC_AUTH0_DOMAIN=your-domain.auth0.com
# NEXT_PUBLIC_AUTH0_CLIENT_ID=your-auth0-client-id
# AUTH0_CLIENT_SECRET=your-auth0-client-secret

# Firebase Configuration (optional - if using Firebase)
# NEXT_PUBLIC_FIREBASE_API_KEY=your-firebase-api-key
# NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
# NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id

# Clerk Authentication (NEW - for POC)
# Get these from https://dashboard.clerk.com after creating an application
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your-clerk-publishable-key
CLERK_SECRET_KEY=sk_test_your-clerk-secret-key

# Clerk URLs Configuration
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/clerk-sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/clerk-sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/onboarding
